augmentation_level: basic
batch_size: 4
ce_weight: 1.0
data_root: H:\LiTS
dice_weight: 0.0
early_stopping_metric: combined_dice
early_stopping_patience: 15
epochs: 100
experiment_name: ablation_ceonly
fold_id: 0
img_size: 256
in_channels: 3
lr: 0.001
model_name: unet
n_folds: 1
num_classes: 3
num_workers: 0
save_dir: models/ablation
save_metric_liver_weight: 0.4
save_metric_tumor_weight: 0.6
scheduler_gamma: 0.1
scheduler_name: steplr
scheduler_step_size: 20
seed: 3407
tumor_weight: 3.0
use_amp: true
weight_decay: 0.0
